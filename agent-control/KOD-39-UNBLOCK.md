# KOD-39 — Autonomous Unblock Directive

Date: 2026-08-07
Owner: Ocín / KODEX−∞
Target agent: Gemini Antigravity (or any coding agent that picks up KOD-39)
Status: ACTIVE DIRECTIVE

## Authority

Proceed autonomously. Do **not** wait for user input for ordinary implementation choices.

The active `KOD-39.yaml` packet remains the primary task contract. If this directive conflicts with an explicit requirement in that packet, the packet wins and the conflict must be reported.

## Current observed block

The agent has already:
- analyzed `~/.gemini/antigravity/`;
- analyzed `KOD-39.yaml`;
- analyzed `~/wenu-frontend/src`;
- entered a `Searching DNA` phase;
- then stopped at `Waiting for user input` without a demonstrated hard blocker.

That waiting state is not sufficient reason to stop.

## Execution rule

1. Re-read `KOD-39.yaml` completely.
2. Inspect the current working tree and relevant KODEX architecture before editing.
3. Resolve ordinary ambiguity from repository evidence, KODEX canon, and the task packet.
4. Search/reuse existing KODEX DNA/helix/spiral/particle/shader/Canvas/WebGL infrastructure before creating parallel systems.
5. Execute the packet end-to-end rather than stopping after research.
6. Preserve unrelated/pre-existing dirty files.
7. Never use destructive reset, mass deletion, or overwrite unrelated work.
8. Reuse shared Effect Foundry/runtime contracts where the packet fits them rather than forking renderer logic.
9. Run appropriate build/typecheck/tests and visual verification available in the local environment.
10. Fix task-caused failures autonomously and retest.
11. Record actual evidence and status; do not equate written code with tested code or tested code with deployed code.

## DNA visual direction

The current KODEX visual material includes the concept **DNA ASCENT / RECORRIDO DEL CÓDIGO VIVO**: a luminous vertical double helix inside a cosmic/technical chamber, with the visitor positioned as observer/participant. Treat this as visual direction only unless `KOD-39.yaml` explicitly promotes it into the task contract.

Potential interaction vocabulary, only when compatible with the packet:
- ascending/descending helix motion;
- depth/parallax around a vertical axis;
- pointer/touch response;
- signal/pulse modulation;
- restrained particle field;
- reduced-motion fallback;
- mobile performance discipline.

Do not present biological, quantum, ancestral, or metaphysical language as scientific fact. KODEX metaphor and scientific claims remain separate.

## Hard blockers that justify stopping

Stop and request Ocín only when one of these is true:
- an unavailable credential/secret is strictly required;
- an irreversible or destructive operation is required;
- two explicit requirements in `KOD-39.yaml` directly contradict each other and repo evidence cannot resolve them;
- a paid/external service purchase is required;
- production deployment requires explicit owner approval.

Everything else: decide, implement, validate, continue.

## Required completion handoff

```yaml
task: KOD-39
objective:
status:
assumptions:
files_changed:
existing_files_reused:
implementation:
tests_run:
test_results:
visual_validation:
known_issues:
deployment_status:
next_action:
```

## Production truth rule

Use the project epistemic distinction:
- discovered code ≠ newly implemented;
- implemented ≠ tested;
- tested ≠ deployed;
- deployed requires direct evidence.

Continue from the **current working tree**. Do not restart from scratch.

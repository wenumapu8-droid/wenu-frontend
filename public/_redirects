# Cloudflare Pages _redirects
# Docs: https://developers.cloudflare.com/pages/configuration/redirects/
#
# ─────────────────────────────────────────────────────────────────────
# STATUS: PRE-CUTOVER (Fase B - soft launch en new.wenumapuonline.com)
# ─────────────────────────────────────────────────────────────────────
# Todas las reglas están comentadas porque WooCommerce aún vive en
# www.wenumapuonline.com. ACTIVAR (descomentar) sólo cuando Fase C esté
# completa y la tienda esté en shop.wenumapuonline.com.
#
# Plan completo: ~/.claude/plans/como-puedo-avanzar-para-lexical-book.md
# ─────────────────────────────────────────────────────────────────────

# === FASE D — ACTIVAR EN CUTOVER AL APEX ===
# Preserva SEO de URLs viejas de WordPress/WooCommerce redirigiendo
# tráfico legacy a la tienda en su nuevo subdominio.
#
# /product/*       https://shop.wenumapuonline.com/product/:splat  301
# /product-category/*  https://shop.wenumapuonline.com/product-category/:splat  301
# /cart            https://shop.wenumapuonline.com/cart            301
# /checkout        https://shop.wenumapuonline.com/checkout        301
# /my-account/*    https://shop.wenumapuonline.com/my-account/:splat  301
# /wp-admin/*      https://shop.wenumapuonline.com/wp-admin/:splat 301
# /wp-login.php    https://shop.wenumapuonline.com/wp-login.php    301
#
# NOTA: /shop NO redirige — es una página real Astro (catálogo vitrina).
# Los CTAs en /shop y /p/[slug] apuntan al checkout de shop. via links
# absolutos en el código (no via _redirects).

# KODEX runtime graph boundary

This runtime surface follows the canonical topology in `wenumapu8-droid/kodex-minus-infinity`:

- `data/alphabet-topology.json`: A is the common origin, M is optional/distributed, Y is route-derived Return; B–L and N–X meanings remain unresolved until creator-approved.
- `data/experience-graph.json`: the first executable slice requires at least six consequential coordinates and state-dependent edges.
- Bridge 1 (`data/bridges/bridge-1-v0`) is a provenance/knowledge graph input, not an implicit visitor-navigation graph.

Therefore:

1. `knowledgeAdjacency` may retain research/content relations.
2. `adjacency` contains only explicitly reviewed `runtimeNavigable` edges.
3. M and Y may be structural runtime endpoints without assigning a Bridge-1 corpus node.
4. Resolver code must refuse projections with contract anomalies.
5. No B–X semantic assignment is inferred by this module.

Deployment is outside this contract and remains separately gated.

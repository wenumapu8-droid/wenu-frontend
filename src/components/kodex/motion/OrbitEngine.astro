---
const {
  scene = 'threshold',
  class: className = '',
  variant = 'threshold',
} = Astro.props;
---
<div class={`kdx-orbit-engine kdx-orbit-engine--${variant} ${className}`.trim()} data-kdx-orbit-engine data-kdx-scene={scene} aria-hidden="true">
  <slot />
</div>

---
const {
  scene = 'threshold',
  class: className = '',
  label = 'particle field',
} = Astro.props;
---
<canvas class={`kdx-particle-field ${className}`.trim()} data-kdx-particle-field data-kdx-scene={scene} aria-label={label}></canvas>

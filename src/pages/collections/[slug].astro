---
// Deprecated 2026-06-23: the invented editorial collections (Renewal Portal, etc.)
// were removed. The REAL brand collections live at /collection/[slug]
// (data/collections.ts). This dynamic route now generates no pages.
export function getStaticPaths() {
  return [];
}
---

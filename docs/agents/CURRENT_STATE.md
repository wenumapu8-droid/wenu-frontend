# CURRENT STATE — KODEX−∞ + WËNU MAPU

Last verified: 2026-08-16.  
Purpose: fast operational truth for agents. This file is **not canon**; re-check Drive canon, active PR heads and exact CI/artifacts before time-sensitive work.

---

## 1. Repository / release boundary

Primary repository: `wenumapu8-droid/wenu-frontend`.

WËNU MAPU production remains on the existing Astro/Cloudflare workflow. Git commits or draft PRs do not authorize merge, deployment, permission changes, public asset approval or canon promotion.

No protected Ocín source original may be modified, cropped, recolored, distorted or treated as publicly approved merely because it appears in a registry or internal lab.

---

## 2. Authoritative technical baseline — PR #62

PR #62 / `feat/kodex-observer-scale-route-v1` remains the verified Deep Navigation + Assembly OS baseline.

Verified head: `1bfc5e4bd0b2fca1d7529dbfffee7b44b6334f20`.  
KODEX Core Runtime run 164: **SUCCESS**.

Already implemented and tested there:

- deliberate browser focus restoration + history/deep-link behavior;
- ElementContract + 28 normalized existing repo-native elements;
- PlateSpec for KNOWLEDGE_PLATE / JUNCTION_PLATE / ACTIVATOR_PLATE;
- deterministic seedable registered-ID-only assembler;
- 12 Golden Plates spanning Science / Technology / Art / Consciousness;
- source-linked copy-role compiler;
- protected Ocín activation adapter with full-view/no-crop/source-integrity gates;
- Assembly QA result contract separating CONTRACT from RENDER_BROWSER truth;
- WorkOrder / StationResult interchange contracts for SCRIPT / AGENT / HUMAN workers;
- macro chapter factory reusing Deep Route Engine;
- seeded factory benchmark;
- universal PlateSpec renderer + desktop/mobile/reduced-motion Golden browser evidence.

`human_curator_acceptance` remains separate from machine validity. Do **not** recreate routing, PlateSpec, assembler, copy, memory, QA or release architecture unless a demonstrated contract gap exists.

### Golden Plate readability audit — run 164

The exact run-164 artifact `kodex-evidence` id `9231076155`, digest `sha256:4bd7761aa06e9c480402b122c4d44dd83766b9177245288a951574a58b0d7180`, has now been downloaded and visually inspected. Browser/contract validity is unchanged and `human_curator_acceptance` remains `NOT_RUN`.

Representative samples expose a bounded curatorial gap rather than a correctness failure: `GP-SCI-01` and `GP-TECH-02` converge on nearly the same sparse circles/links + dossier composition; `GP-CON-03` is legible but remains diagrammatic/scaffold-like; `GP-ART-03` correctly preserves source-withheld protected-art state, so actual artwork materiality/no-crop cannot be visually judged without explicit source-byte authorization. Macro title hierarchy is strong; composition/material/payload differentiation is the limiting issue.

The SCI/TECH cause is now localized in code. `deterministic-assembler.js` already resolves an eligible registered composition into `slots[].element_id`, and the existing registry already contains differentiated ARCHIVE-compatible Knowledge compositions such as `KDX_G01_ORBITAL_RELIC`, `KDX_G02_SPECIMEN_DOSSIER` and `KDX_G07_ARCHIVE_DOSSIER`. The universal `KodexPlateSpecRenderer.astro`, however, currently renders every KNOWLEDGE_PLATE through the same hard-coded `.kdx-golden__diagram` structure and exposes the selected element mainly as metadata. This is a **renderer-consumption gap**, not a schema/registry/assembler failure.

Exact benchmark dispatch is now pinned: with the existing seeds and assembler, `GP-SCI-01 / SCI-BIOLOGY / seed-gp-sci-01` selects **`KDX_G07_ARCHIVE_DOSSIER`**, while `GP-TECH-02 / TECH-MACHINE / seed-gp-tech-02` selects **`KDX_G02_SPECIMEN_DOSSIER`**. Their registry/grid contracts are materially distinct: G07 requires unequal hero/record hierarchy + signal bridge + status cell; G02 is a specimen/provenance dossier with a wider command rail and payload-first narrow-screen recomposition. Their current visual convergence is therefore proof that the renderer collapses distinct selected composition contracts.

The current Golden Playwright gate checks registry-addressable `elementId`, 100dvh/no-scroll, mobile, focus, reduced motion, performance and protected-art boundaries, but it does not yet prove structural dispatch. After Actions execution is available, the bounded #62 change is exactly: consume existing G01/G02/G07 Knowledge composition semantics in `KodexPlateSpecRenderer.astro` and extend `scripts/kodex-golden-plate-browser-evidence.mjs` so selected composition IDs produce explicit distinguishable rendered structure/fingerprints. Do not change assembler scoring or invent IDs to force difference.

This is agent perceptual/readability evidence, not creator acceptance and not a reason to introduce another renderer, schema, route, memory or factory. `human_curator_acceptance` remains `NOT_RUN`.

PR #62 remains OPEN / DRAFT / NOT MERGED / NOT DEPLOYED. Runtime mutation is deferred while exact-head GitHub Actions validation remains blocked at the account billing/spend gate.

---

## 3. Perceptual / HoloCore lanes

### PR #70 — current volumetric benchmark

Verified head: `0676c9e8a4d39c3b8949548598f9c021c87e3e3c`.  
KODEX Core Runtime run 278 / `31860800425`: **SUCCESS**.  
Artifact `9240563358`, digest `sha256:bd87510716dc459c65c28f27e8cada7270de0755a57e29c52bf9176a2831707c`.

The TOROIDAL FIELD remains an internal `ART / COMP / SPEC` SOURCE_GENERATOR on the existing WebGL source-renderer contract. The raymarch discontinuity defect is closed; actual artifact review shows one continuous closed torus with preserved aperture/front-back separation. Creator visual acceptance remains **PENDING**. It is not scientific field simulation and not final visual canon.

### PR #73 — RGX family baseline

Reviewed head `4b209ef711a62442c64f22d898825ca5c5424bd9`, run 266 SUCCESS. RGX remains **ADAPT / specialized structural grammar**, not default/final materiality. Do not expand family scope from CI alone.

### PR #75 — targeted reference-grounded RGX correction

Verified head `8959a0568f54bec465601093f62c96a590b49f2f`, run 270 SUCCESS. SIGNAL SEED and LIVING ORGANISM are strong creator-review candidates; HEART CHAMBER and SKULL ARCHIVE are materially improved but deliberately schematic. Source pixels remain NONE. This supersedes the older #73 readability hold for those four review surfaces only; it does not promote RGX as a family.

### PR #76 — systems convergence substrate

Verified head `d15bad99f52f38f831cd7ec0f2e4bd157e460c26`, run 272 SUCCESS. It converges Manifestation state, JourneyState semantic memory, Assembly WorkOrder and RGX/HoloCore without creating parallel state/memory/route/renderer systems. Keep it as a tested substrate, not a new product corridor.

---

## 4. Product-facing corridor — PR #77

PR #77 / `feat/kodex-threshold-convergence-v1` advances the existing #68 corridor in place. It must not silently substitute for #62/#76 architecture or reimplement their systems.

### Browser-validated rooms

The following rooms have exact-head Product Corridor acceptance and artifact review on this draft lineage:

- **THRESHOLD** — bounded 100dvh/no-scroll, WebGL portal or source fallback, existing `kx-journey` memory, explicit visitor-controlled ENTER navigation.
- **PROLOGUE** — bounded title/CTA/art geometry, real CTA hit-testing, CRT/fallback, protocol focus restoration, memory, explicit BEGIN OBSERVATION navigation.
- **DESCENT** — local 11-stratum state is user-controlled; `DESCEND` changes depth while remaining on `/kodex/folio/ii/`; only explicit shared NEXT exits. Desktop/mobile/reduced-motion accepted.
- **ARCHIVE** — selected specimen + dossier + focus restoration + memory + explicit NEXT accepted. Narrow mobile hides only the redundant technical rail; desktop/reduced-motion retain it.

Latest verified four-room head remains `99320ff527a73a0f53e145662ae46141eb5bd8c8`, Product Corridor run 27 / `31875814017` SUCCESS, artifact `9244748394`, digest `sha256:1818401a22618dd7e1c461b7fdba0dd01f0ea93e38c193d92eaf922f38b507f8`.

Creator aesthetic acceptance remains separate from browser acceptance.

---

## 5. MACHINE — current truth gate

Concurrent work already added MACHINE evidence to the existing PR #77/Product Corridor workflow. Do not create another MACHINE gate or workflow.

### Failing evidence that exposed a real truth defect

Head `cd166a43c7c930beedde587f9afb14295a8b73bf` reached the MACHINE gate in Product Corridor run `31876303307` and was rejected because the scene presented `INTEGRITY · 98.7%` as factual telemetry without a VERIFIED runtime measurement/source.

Evidence artifact:

- `kodex-product-corridor-evidence`
- id `9244891650`
- digest `sha256:b240efe942a0d4f8006cbc192fedceb6df57e5f91f8f213b89a8150c4fce59c7`

This is the desired behavior of the truth/provenance gate. Do not weaken it.

### Current source repair

Current PR #77 head: `1f7bbebcf7573811549b540c573ad4b16919fa46`.

Only two source truth labels changed in `src/pages/kodex/folio/[folio].astro`:

- `INTEGRITY · 98.7%` → `INTEGRITY · NOT MEASURED`
- `98.7%` → `NOT MEASURED`

Exact compare from `cd166a43...` to `1f7bbeb...`: **one file, +2 / -2**. Generator, renderer, routing, memory, artwork and composition are unchanged.

A first edit attempt touched unrelated comments in the shared monolithic folio file. That collateral edit was detected by compare and fully reverted before the final two-line repair. Do not resurrect commit `25a2737...`; it is not the accepted head.

`NOT MEASURED` is an explicit epistemic state, not substitute telemetry.

### Current CI blocker — exact provider cause

KODEX Product Corridor run `31877581094` / #32 targets exact head `1f7bbeb...`. Attempt **6**, job `95021079046`, again completed before a workflow step executed: `runner_id=0`, empty runner and `steps=[]`.

The GitHub Actions check-run exposes the provider cause explicitly:

> The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings.

The current blocker is **GitHub account billing / Actions spending authorization**, not KODEX product code and not MACHINE browser evidence.

MACHINE remains:

**IMPLEMENTED TRUTH REPAIR / BROWSER REVALIDATION PENDING**

Do not call MACHINE browser-accepted. Do not modify product code, tests, workflow logic or QA thresholds to compensate for this account-level block.

---

## 6. Fragmentation / source-of-truth warning

Open PR status alone does not make a branch authoritative.

- #62 = verified Deep Navigation + Assembly OS baseline.
- #70 = current browser-validated volumetric/materiality benchmark.
- #73 = RGX technical-family baseline; specialized structural grammar.
- #75 = targeted reference-fidelity creator-review surface for Heart/Skull/Signal Seed/Living Organism.
- #76 = verified systems-convergence substrate.
- #68 = product corridor lineage; still separate from #62/#76.
- #77 = bounded product-corridor acceptance lane on #68 lineage.

No standalone current Decision Log was found in accessible Drive or repo search. Do **not** invent one. Operational truth comes from Drive canon + this Current State + exact active PR heads/CI/artifacts.

---

## 7. Strategic condition

KODEX already has enough architecture to proceed. Primary risks are now:

- mistaking CI/browser green for creator acceptance;
- adding architecture instead of converging existing systems;
- fabricating telemetry or letting decorative numbers read as facts;
- scaling renderer families before their perceptual role is accepted;
- confusing scientific fact, authored metaphor, cultural provenance, inference, implementation and deployment state;
- treating protected artwork curation as public-use approval.

Strategic mode: **convergence + curation + perceptual refinement**.

`BUILD LESS. CURATE HARDER. LET MEMORY CREATE COMPLEXITY.`

---

## 8. Exact next action

1. Preserve PR #62 as the verified Assembly/Deep Navigation baseline. Do not reopen P0.1–P0.10.
2. Keep the exact Golden renderer dispatch contract pinned: `GP-SCI-01 → KDX_G07_ARCHIVE_DOSSIER`; `GP-TECH-02 → KDX_G02_SPECIMEN_DOSSIER`. Their current identical Knowledge markup is a renderer-consumption defect, not assembler diversity failure.
3. Keep PR #77 on exact head `1f7bbeb...`; do **not** advance COSMOLOGY yet.
4. Resolve the GitHub `Billing & plans` / Actions spending-limit problem outside the codebase. This is an account-level prerequisite, not a KODEX code task.
5. After billing/spend authorization is restored, re-run the same exact Product Corridor head without product-code changes.
6. If MACHINE executes and passes, inspect the exact desktop/mobile/reduced-motion artifact before changing its state to browser-accepted.
7. If MACHINE executes and fails, repair only the measured defect and retain truth/provenance gates.
8. After CI availability is restored, advance #62 **in place** with the bounded existing-renderer delta only: dispatch existing G01/G02/G07 Knowledge composition semantics and extend Golden browser evidence with structural identity/fingerprint checks proving selected registered compositions no longer collapse to identical DOM structure. No new IDs, no assembler-scoring changes and no parallel renderer.
9. Preserve user agency, keyboard/focus, reduced-motion/fallbacks, 100dvh/no-scroll, cultural/source provenance and Ocín artwork integrity.
10. No merge, deploy, permission change, public asset approval or canon promotion is authorized.

---

## 9. Session-start checks

Before editing KODEX, verify:

1. Which Drive canon applies?
2. Which PR is the newest verified owner of the capability?
3. Is the requested delta architectural, semantic, visual, authorial or deployment-related?
4. Does a reusable implementation already exist?
5. What exact CI/browser/**visual artifact** evidence exists for the current head?
6. Is creator/cultural/public-release approval required?
7. Does the change preserve user agency, reduced-motion/fallbacks and truth-state separation?
8. Is the output implementation, test evidence, hypothesis, reference or deployment?

If these cannot be answered from connected sources, do not guess.

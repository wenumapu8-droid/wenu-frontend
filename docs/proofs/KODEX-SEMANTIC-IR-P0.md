# KODEX Semantic IR P0 — Principle → Manifestation

Status: `IMPLEMENTED_CANDIDATE / CI_PENDING / INTERNAL / NOINDEX / NOT_CANON / NOT_DEPLOYED`

## Question

Can KODEX express a principle once as a semantic contract and materialize it through the existing Assembly OS without creating a page-specific architecture?

## Architecture boundary

```text
QUESTION / PRINCIPLE
        ↓
KODEX SEMANTIC IR
        ↓
PRE-ASSEMBLY ADAPTER
        ↓
existing assemblePlateSpec(...)
        ↓
existing PlateSpec
        ↓
existing compilePlateRenderModel(...)
        ↓
existing KodexPlateSpecRenderer
```

Semantic IR is not a renderer, route engine, memory store, JourneyState replacement, ElementContract dialect, PlateSpec dialect or canon authority.

## P0 fixtures

1. `IMPERMANENCE`
   - operators: `MANIFEST → MUTATE → ERODE → INHERIT`
   - depth: `LINEAGE / INFORMATION`
   - memory: `RESIDUE`
   - return: `RETURNED_FORM`
   - materializes through an existing `KNOWLEDGE_PLATE / ARCHIVE` path.

2. `INTERDEPENDENCE`
   - operators: `REVEAL → RELATE → DIVERGE → CONVERGE`
   - depth: `SYSTEMIC / EPISTEMIC`
   - memory: `ROUTE_SIGNATURE`
   - return: `THRESHOLD_PRIME`
   - materializes through an existing `JUNCTION_PLATE / MACHINE` path with explicit bounded routes.

3. `OBSERVER`
   - operators: `OBSERVE → REVEAL → INHERIT → RETURN`
   - depth: `EPISTEMIC / META`
   - memory: `TRACE`
   - return: `TRACE_VISIBLE`
   - materializes through an existing governed living-field `ACTIVATOR_PLATE / COSMOLOGY` path.

## Protected source boundary

Semantic IR P0 refuses direct protected `ARTWORK` admission. Protected Ocín sources remain governed by the existing protected activation adapter and source-integrity contracts.

## Internal review route

`/kodex/lab/semantic-ir/`

The route is `noindex, nofollow` and is a proof surface only. It renders the three fixtures using the existing PlateSpec renderer and overlays the semantic trace so the chain from principle to manifestation remains inspectable.

## Acceptance

P0 can advance only if:

- all three fixtures validate;
- same semantic IR + same seed produces the same PlateSpec;
- no unregistered semantic operator passes validation;
- Junction route choice remains explicit and bounded;
- Observer remains a governed living field, not a protected-art bypass;
- existing Assembly OS contract tests remain green;
- Astro build passes;
- the internal proof route renders through existing components;
- no deployment or canon promotion is inferred.

## What this P0 does not prove

- that the selected 13-operator vocabulary is final;
- that the three principles are universal scientific laws;
- that spiritual, philosophical and scientific statements are equivalent;
- that memory/return consequences are yet fully wired into the public journey;
- that the rendered manifestations have creator visual acceptance;
- that Semantic IR should be public API/SDK.

## Success criterion

The stone-corner hypothesis is supported if a principle can be specified once, retain provenance/epistemic/operator/memory/return intent as semantic trace, and enter the existing deterministic manifestation chain without a custom renderer or custom runtime for that principle.

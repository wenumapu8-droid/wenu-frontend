# KOD-49 — THRESHOLD Recovery Inventory v1

Status: `RECOVERY LANE ACTIVE`

Objective for this lane is singular: move **THRESHOLD** toward creator-accepted 100% by reusing already-authored KODEX assets before generating or reinventing any visual engine.

## Recovered assets now accounted for

### Open Visual Lab V1
Previously authored dependency-free WebGL2 system with:
- `KodexPipeline.js` runtime contract;
- `threshold-portal.frag`;
- `post-kodex.frag` treatment layer;
- temporal feedback;
- KodeLife-compatible GLSL;
- DPR/mobile/reduced-motion guidance.

The authored `threshold-portal.frag` has now been copied verbatim into this recovery lane as:

`public/assets/kodex/shaders/threshold-portal-recovered.frag.glsl`

A local fullscreen vertex pass is supplied at:

`public/assets/kodex/shaders/threshold-recovery-fullscreen.vert.glsl`

### Existing KODEX runtime patterns
Do not invent WebGL lifecycle from prose. Reuse the tested patterns already present in KODEX packages / repo history:
- WebGL2 compile/link;
- mobile DPR caps;
- pointer smoothing;
- reduced motion;
- IntersectionObserver pause;
- visibility pause;
- context-loss fallback;
- resource disposal.

The DNA Passage branch provides a current repo-native WebGL2 lifecycle reference. Historical KODEX packages also provide Ripple Floor, Wrinkled Reality, Split Corridor, Perspective Flip and Observe runtime contracts.

### Effect Foundry
`effectFoundry.js` / `effectFoundryRuntime.js` already contain KDX-FX-001…006. Preserve these as useful Canvas2D/fallback/effect layers. Do not mistake their current Canvas2D implementations for the final reference-level GPU material pass.

### Visual target / golden reference
Use the approved THRESHOLD references and Visual Atlas as the diff target. Important abstract properties:
- tall/dominant portal organism;
- central aperture;
- asymmetric organic material rather than a generic radial loader;
- black/obsidian field with mineral violet/chrome signal;
- side protocol / dense editorial grammar;
- DORMANT → AWARE → OPEN;
- explicit voluntary crossing.

Never copy fabricated telemetry from concept boards into product state.

## Recovery comparison route

`/kodex/lab/threshold-recovery/`

This route is noindex and non-production. It mounts the recovered shader through a repo-native WebGL2 feedback runtime, leaving the existing Canvas2D fidelity candidate untouched for A/B comparison.

## Architecture for final convergence

Target final stack:

```text
stable semantic DOM / JourneyState
  + approved KODEX poster grammar
  + recovered GPU Threshold shader / tuned native successor
  + temporal feedback / material treatment
  + pointer/touch state response
  + current Canvas2D / static fallback
```

The shader owns visual state only. Navigation/history/memory remain outside WebGL.

## What counts as progress today

Discovery alone does not increase completion percentage.

THRESHOLD reaches 100% only after:
1. recovered GPU candidate builds and opens;
2. direct comparison against approved Threshold reference;
3. visual impact >= 90/100 or creator explicitly accepts equivalent result;
4. native implementation >= 85/100 with no flattened full-screen image dependency;
5. desktop + 390×844 + 412×915 QA;
6. FULL / REDUCED / OFF or equivalent accessible fallback behavior;
7. JourneyState / semantic entry integration remains correct;
8. creator visual approval.

Production publication is a separate gate and remains blocked without exact `APROBAR DEPLOY`.

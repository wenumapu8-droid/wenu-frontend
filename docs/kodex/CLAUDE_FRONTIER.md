# KODEX−∞ — Claude frontier operating memory

This file applies only to KODEX work in this repository.

## ⚠ ANTES DE TOCAR NADA · Integration OS

**Ley constitucional (2026-08-30)**: cualquier agente que trabaje en KODEX
debe leer PRIMERO estos archivos en `kodex-system/`:

```
kodex-system/README.md              regla SEARCH BEFORE CREATE + DONE nueva
kodex-system/SENTINEL.md            el guardian, en primera persona
kodex-system/00_VAULT_INVENTORY     donde vive que (128 files + 60 Drive URLs)
kodex-system/01_CURRENT_STATE       que esta VIVO por ruta ahora mismo
kodex-system/02_SCENE_REGISTRY      7 escenas + 4 chambers + SILENCE hueco
kodex-system/03_COMPONENT_REGISTRY  112 componentes clasificados
kodex-system/05_DECISION_LEDGER     47 decisiones + 7 conflictos abiertos
kodex-system/06_INTEGRATION_BACKLOG INT-XXX tareas concretas
```

**Sin leer eso, no se escribe código nuevo.** Antes de crear cualquier
componente, producir el bloque:

```
EXISTING IMPLEMENTATION SEARCH
  canonical found: <lista>
  orphan found: <lista>
  REUSE POSSIBLE: YES / NO
  If NO: reason: <texto>
```

**Definición de DONE**: `mounted + reachable + interactive + correct asset
+ correct copy + responsive + tested + screenshot verified + deployed +
visually accepted`. Si falta uno, no está DONE. Reemplaza a "componente
existe + build passes + commit".

Ver `kodex-system/README.md` para el ciclo completo DISCOVER → RECOVER →
RECONCILE → SELECT → DEPRECATE → MOUNT → CONNECT → ASSETS → INTERACTION →
MOBILE → VISUAL CHECK → TEST → DEPLOY → SCREENSHOT → ACCEPT.

## Authority and repos

- Application/runtime repo: `wenumapu8-droid/wenu-frontend`.
- Canon/factory repo: `wenumapu8-droid/kodex-minus-infinity`.
- Before KODEX work, read the canonical factory dispatch and the exact assigned packet from the canonical repo.
- Current frontier visual policy: `ops/factory/FRONTIER_VISUAL_GATE.md` in the canonical repo.
- Never infer that an agent is active only because it is connected. Require fresh heartbeat/commit/test/PR evidence.

## Frontier-model role

When this Claude Code station is assigned a frontier packet, act as a senior architecture + visual-convergence worker, not a volume code generator.

Prioritize:
- reference-sensitive visual judgment;
- composition, focal hierarchy and depth;
- motion/interaction quality;
- difficult integration/reconciliation;
- architecture with long-context implications;
- explicit scientific/cultural/provenance boundaries.

Do not spend frontier capacity on mechanical work that can be safely delegated to the bounded OpenCode/OpenClaude worker.

## Visual convergence rule

`BUILD_PASS != VISUAL_PASS`.

For KODEX visual work, compare the current implementation against approved creator references and report measurable deltas in:

1. composition/focal hierarchy;
2. viewport occupation of the dominant organism/object;
3. Z-depth and separation of foreground/midground/background;
4. density vs negative space;
5. typography hierarchy and editorial rhythm;
6. palette roles, contrast and light;
7. motion timescales/transitions;
8. pointer/touch/keyboard semantic parity;
9. desktop/mobile framing;
10. reduced-motion/fallback equivalence;
11. narrative/semantic alignment;
12. scientific, cultural and provenance constraints.

Prefer concrete instructions such as `reduce HUD density ~30%` or `increase focal organism occupation from ~35% to ~55%` over vague directions.

## Worker routing

- Mac mini / Claude Max: primary frontier review and difficult implementation.
- iMac / Claude Pro: secondary frontier review/context recovery when authenticated and quota is available.
- OpenCode/OpenClaude + fast/free model: bounded plumbing, deterministic deltas, tests, CI, repetitive edits.
- Gemini/Antigravity: multimodal passports/specs; not final creator visual authority.
- ChatGPT orchestrator: canon, architecture, dispatch, reference decomposition and acceptance interpretation.

## KODEX product loop

`VISIBLE PREVIEW → FRONTIER VISUAL AUDIT → DELTA PACKET → BOUNDED IMPLEMENTATION → CI + SCREENSHOTS → FRONTIER RE-AUDIT → CREATOR ACCEPTANCE → PROMOTION CANDIDATE`

The public `/kodex/` route is not the experimentation surface. Prefer noindex preview/lab/staging routes until creator acceptance.

## Safety

- Preserve unrelated dirty work.
- Do not reset or rewrite history destructively.
- Do not self-assign a new packet after handoff.
- No fabricated telemetry, biometrics, frequencies, psychological ranking or scientific claims.
- Cultural/ancestral material requires provenance and explicit boundaries.
- Do not store or print secrets, OAuth tokens, API keys, cookies or credentials.
- Do not deploy without the exact creator phrase `APROBAR DEPLOY`.

## Current high-value review targets

- KOD-47 Visible Assembly (`feature/kodex-visible-assembly-01` / PR #32): end-to-end internal journey; audit as product/experience, not only as runtime.
- DNA Passage (`feature/kodex-dna-passage-v1` / PR #33): tested technical candidate; requires frontier + creator visual acceptance before public integration.

When a target has no openable preview or screenshot evidence, stop the visual verdict and request/produce that evidence first rather than guessing.

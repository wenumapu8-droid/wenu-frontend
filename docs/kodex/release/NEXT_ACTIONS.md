# NEXT ACTIONS — KODEX Release 2026-08-21

1. Keep `51ef74f2a754d8fb10855a5fb9da231afa759583` / KODEX Core Runtime #385 (run `32555582558`) as the latest exact-head CI/browser baseline observed on the active PR #101 lineage. This is technical evidence only; it is not creator visual KEEP, canon, merge or deployment. Documentation-only commits do not displace this product baseline.
2. Do not rebuild Assembly OS / Deep Navigation P0.1–P0.10. PR #62 is closed baseline infrastructure; correctness continues to come from the existing registries, schemas, constraints, validators and current JourneyState/graph-memory authorities.
3. Advance the existing PR #101 lineage only. Historical PRs/branches remain bounded visual source material; port only the smallest useful presentation delta into the current host.
4. Continue the live opening corridor as one audiovisual traversal: `THRESHOLD → PORTAL transition → PROLOGUE → first depth of DESCENT`. The user should read this as movement inward through one world, not page replacement.
5. Keep artwork/image/organism dominant before interface. Technical UI, routes, state labels, metadata, rails and instrumentation remain available for function/accessibility but subordinate to the visible universe: `HIDE THE SYSTEM → SHOW THE UNIVERSE`.
6. The next product-facing delta must improve transition continuity rather than add architecture: temporal reveal/order, portal→field handoff, multicue depth, image-role changes, integrated affordances and deliberate mobile timing using the existing transition/runtime/CSS hooks. Change the camera/field, not protected originals.
7. Add temporal evidence for the opening corridor before broadening scope: sequential frames or video at 1440×900, 390×844 and 412×915, plus reduced-motion evidence. Static endpoint screenshots are insufficient to judge traversal quality.
8. On the same exact product head, verify 100dvh/no accidental page scroll, keyboard/touch parity, reduced motion, artwork aspect integrity, no first-party browser errors, and no consequential URL/history/memory write before explicit visitor choice.
9. Use temporal evidence to identify the smallest visible defect, then repair only that defect. Do not add a second router, memory store, scene registry, semantic graph authority, Assembly OS, renderer family, or engagement/readiness/coherence scoring.
10. After the opening corridor is creator-reviewed, generalize only the proven primitives to ARCHIVE → MACHINE → COSMOLOGY → RETURN, preserving distinct scene organisms and the existing seven-scene spine.
11. Keep cross-device QR continuation as a separate bounded lab until the opening corridor is stable. Reuse JourneyState; never expose raw private session/JourneyState identifiers in QR URLs.
12. Release Candidate remains evidence-gated: complete journey, authored desktop/mobile behavior, state-safe transitions, performance/accessibility/provenance gates, exact-head browser evidence and explicit creator acceptance. No public deployment without separate release authority.
